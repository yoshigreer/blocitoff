<%= form_for user_items_path(user_id: @user.id) do |f| %>
  <div class="form-group">
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter todo" %>
    <%= f.submit "Add Todo", class: 'btn btn-success' %>
  </div>
<% end %>
